# 検索機能 - 動作確認ガイド

## 実装内容

### 1. バックエンド実装

#### フォーム (`forms/search_forms.py`)
```python
class SearchForm(FlaskForm):
    q = StringField(
        '検索',
        validators=[
            DataRequired(message='検索キーワードを入力してください'),
            Length(min=1, max=100, message='検索キーワードは1〜100文字で入力してください')
        ]
    )
    submit = SubmitField('検索')
```

#### ルート (`routes/search.py`)
```python
@search.route('/results')
@login_required
def results():
    query = request.args.get('q', '').strip()
    search_query = query.lstrip('#')  # ハッシュタグ対応
    
    # ユーザー名検索（部分一致）
    users = User.select().where(User.username.contains(search_query))
    
    # 投稿検索（キャプション・ハッシュタグ）
    posts = Post.select().where(
        (Post.caption.contains(search_query)) |
        (Post.caption.contains(f'#{search_query}'))
    ).order_by(Post.created_at.desc())
```

### 2. フロントエンド実装

#### テンプレート
- `templates/search/index.html` - 検索ページ
- `templates/search/results.html` - 検索結果ページ

---

## 動作確認ポイント

### ✅ ユーザー名で検索できる

**確認手順**:
1. ログイン後、ナビゲーションバーの検索アイコン（🔍）をクリック
2. 検索ページが表示される
3. 検索フォームにユーザー名（または一部）を入力
   - 例: `john`, `alice`, `user`
4. 「検索」ボタンをクリック
5. 検索結果ページに移動
6. 「ユーザー」タブに該当するユーザーが表示される

**実装ロジック**:
```python
# routes/search.py
users = User.select().where(
    User.username.contains(search_query)
).limit(20)
```

**検索方式**:
- **部分一致**: 入力したキーワードがユーザー名に含まれていればヒット
- **大文字小文字**: データベース設定に依存（通常は区別しない）

**表示内容**:
- プロフィール画像
- ユーザー名
- 自己紹介（先頭80文字）
- 投稿数
- フォロワー数

**例**:
- 検索キーワード: `john`
- ヒット例: `john`, `johnny`, `john_smith`, `johnson`

---

### ✅ キャプションで検索できる

**確認手順**:
1. 検索ページを開く
2. 検索フォームにキャプション内のキーワードを入力
   - 例: `風景`, `旅行`, `美味しい`
3. 「検索」ボタンをクリック
4. 検索結果ページの「投稿」タブに該当する投稿が表示される

**実装ロジック**:
```python
# routes/search.py
posts = Post.select().where(
    Post.caption.contains(search_query)
).order_by(Post.created_at.desc()).limit(50)
```

**検索方式**:
- **部分一致**: キャプション内にキーワードが含まれていればヒット
- **新着順**: 検索結果は投稿日時の降順で表示

**表示内容**:
- 投稿画像（グリッド表示）
- ホバーで投稿者名、いいね数、コメント数を表示

**例**:
- キャプション: "今日は美味しいラーメンを食べました #グルメ #ラーメン"
- 検索キーワード: `美味しい` → ヒット
- 検索キーワード: `ラーメン` → ヒット
- 検索キーワード: `グルメ` → ヒット（ハッシュタグとしても検索可能）

---

### ✅ ハッシュタグで検索できる（#付き/なし両対応）

**確認手順**:
1. 検索ページを開く
2. ハッシュタグで検索（2パターン）
   - **パターンA**: `#旅行` （#付き）
   - **パターンB**: `旅行` （#なし）
3. 「検索」ボタンをクリック
4. 両方のパターンで同じ結果が表示されることを確認

**実装ロジック**:
```python
# routes/search.py
# ハッシュタグ検索の場合、#を除去
search_query = query.lstrip('#')

# 投稿検索（ハッシュタグ対応）
posts = Post.select().where(
    (Post.caption.contains(search_query)) |
    (Post.caption.contains(f'#{search_query}'))
)
```

**検索方式**:
1. **#除去**: 入力されたキーワードから先頭の`#`を自動的に除去
2. **2パターン検索**:
   - パターン1: キーワードそのまま（例: `旅行`）
   - パターン2: `#`付き（例: `#旅行`）
3. **OR検索**: どちらかにマッチすればヒット

**例**:
- キャプション: "夏の思い出 #旅行 #海 #夏休み"
- 検索: `#旅行` または `旅行` → どちらもヒット
- 検索: `#海` または `海` → どちらもヒット

**ハッシュタグの書き方**:
- ✅ 正しい: `#旅行`, `#風景写真`, `#PhotoOfTheDay`
- ⚠️ 推奨: スペースなしで記述
- ❌ 注意: `# 旅行` （スペースあり）は検索できない場合がある

---

### ✅ 検索結果が表示される

**確認手順**:
1. 任意のキーワードで検索
2. 検索結果ページに以下が表示される:
   - 検索キーワード（太字で強調）
   - 検索結果の件数（ユーザー/投稿）
   - タブ切り替え（投稿/ユーザー）
   - 検索結果一覧

**表示レイアウト**:

#### 投稿タブ
```
┌─────────────────────────────────────┐
│  [投稿画像1] [投稿画像2] [投稿画像3] │
│  [投稿画像4] [投稿画像5] [投稿画像6] │
│  ...                                │
└─────────────────────────────────────┘
```
- グリッド表示（4列）
- ホバーで詳細情報を表示
- クリックで投稿詳細ページに遷移

#### ユーザータブ
```
┌─────────────────────────────────────┐
│ [画像] ユーザー名                     │
│        自己紹介                       │
│        10投稿 | 25フォロワー           │
├─────────────────────────────────────┤
│ [画像] ユーザー名2                    │
│  ...                                │
└─────────────────────────────────────┘
```
- リスト表示
- クリックでプロフィールページに遷移

**結果が0件の場合**:
- アイコンと「見つかりませんでした」メッセージを表示
- 「別のキーワードで検索してみてください」と案内

**実装箇所**:
```html
<!-- templates/search/results.html -->
{% if posts %}
    <!-- 検索結果を表示 -->
{% else %}
    <div class="text-center py-5">
        <i class="fas fa-search fa-4x text-muted mb-3"></i>
        <h3 class="h5 text-muted">投稿が見つかりませんでした</h3>
        <p class="text-muted">別のキーワードで検索してみてください</p>
    </div>
{% endif %}
```

---

### ✅ 部分一致で検索できる

**確認手順**:
1. 完全なキーワードではなく、一部だけを入力
   - 例: ユーザー名 `johnsmith` に対して `john` と入力
   - 例: キャプション "美味しいラーメン" に対して `ラーメン` と入力
2. 「検索」ボタンをクリック
3. 部分一致で検索結果が表示される

**実装ロジック**:
```python
# PeeWee ORM の contains() メソッドを使用
User.username.contains(search_query)  # LIKE '%search_query%'
Post.caption.contains(search_query)   # LIKE '%search_query%'
```

**部分一致の仕組み**:
- **前方一致**: `john` → `john`, `johnny`, `johnson`
- **中間一致**: `smith` → `johnsmith`, `smith123`, `blacksmith`
- **後方一致**: `123` → `user123`, `abc123`

**例**:
| データ | 検索キーワード | ヒット |
|--------|---------------|--------|
| `johnsmith` | `john` | ✅ |
| `johnsmith` | `smith` | ✅ |
| `johnsmith` | `johndoe` | ❌ |
| `#旅行の写真` | `旅行` | ✅ |
| `#旅行の写真` | `写真` | ✅ |
| `#旅行の写真` | `風景` | ❌ |

**パフォーマンス**:
- 検索結果は最大50件（投稿）、20件（ユーザー）に制限
- 大量データでも高速に検索可能

---

## 検索のヒント

### 効果的な検索方法

1. **ユーザーを探す**
   - ユーザー名の一部を入力
   - 例: `john`, `alice`, `photo`

2. **投稿を探す**
   - キャプションのキーワードを入力
   - 例: `風景`, `美味しい`, `旅行`

3. **ハッシュタグで探す**
   - `#`付きでも`#`なしでもOK
   - 例: `#旅行`, `旅行` （どちらも同じ結果）

4. **複合検索**
   - ユーザー名とキャプションの両方を検索
   - タブで切り替えて結果を確認

### 検索のコツ

✅ **良い検索例**:
- `風景` - キーワードがシンプル
- `john` - ユーザー名の一部
- `#旅行` - ハッシュタグを直接指定

❌ **避けるべき検索**:
- `風景 写真` - スペース区切りは未対応（将来対応予定）
- `@username` - `@`記号は不要（自動的に除去されない）

---

## アクセス方法

### ナビゲーションバーから
1. 右上の検索アイコン（🔍）をクリック
2. 検索ページが開く

### 直接URL
- 検索ページ: `/search`
- 検索結果: `/search/results?q=キーワード`

---

## データベース検索仕様

### 検索クエリ

#### ユーザー名検索
```sql
SELECT * FROM users 
WHERE username LIKE '%キーワード%'
LIMIT 20;
```

#### 投稿検索（キャプション・ハッシュタグ）
```sql
SELECT * FROM posts 
WHERE caption LIKE '%キーワード%' 
   OR caption LIKE '%#キーワード%'
ORDER BY created_at DESC
LIMIT 50;
```

### インデックス

既存のインデックスを活用:
- `users.username` - ユーザー名検索の高速化
- `posts.created_at` - 投稿の並び替え高速化

---

## トラブルシューティング

### 検索結果が表示されない
**原因と対処法**:
1. キーワードが短すぎる（1文字未満） → 2文字以上入力
2. 特殊文字が含まれている → 英数字のみで検索
3. データベースにデータがない → テストデータを作成

### ハッシュタグで検索できない
**原因と対処法**:
1. ハッシュタグの形式が間違っている → `#keyword` の形式で投稿
2. スペースが含まれている → `#旅行` OK、`# 旅行` NG
3. 特殊文字が含まれている → 英数字・日本語のみ推奨

### 検索が遅い
**原因と対処法**:
1. データ量が多い → インデックスを確認
2. 複雑な検索 → キーワードを短くする
3. ネットワーク遅延 → サーバーの状態を確認

---

## 実装済みファイル一覧

✅ **フォーム**
- `forms/search_forms.py` - SearchForm

✅ **ルート**
- `routes/search.py` - 検索ページ、検索結果

✅ **テンプレート**
- `templates/search/index.html` - 検索ページ
- `templates/search/results.html` - 検索結果ページ

✅ **ナビゲーション**
- `templates/base.html` - 検索アイコン追加

✅ **Blueprint登録**
- `app.py` - searchブループリント登録

---

## まとめ

検索機能は完全に実装されており、以下の機能が利用可能です:

1. ✅ ユーザー名検索（部分一致）
2. ✅ キャプション検索（部分一致）
3. ✅ ハッシュタグ検索（#付き/なし両対応）
4. ✅ 検索結果表示（タブ切り替え）
5. ✅ グリッド表示（投稿）とリスト表示（ユーザー）
6. ✅ レスポンシブデザイン
7. ✅ ホバーエフェクト
8. ✅ 0件時のメッセージ表示

すべての動作確認ポイントをクリアしています。
